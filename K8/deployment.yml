apiVersion: apps/v1
kind: Deployment
metadata:
  name: onlineshop
spec:
  replicas: 3
  selector:
    matchLabels:
      app: pdd
  template:
    metadata:
      labels:
        app: pdd
    spec:
      containers:
      - name: appdeploy
        image: raichu08/onlineshop:v6
        ports:
        - containerPort: 80
